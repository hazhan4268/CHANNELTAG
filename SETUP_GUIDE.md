# 🚀 راهنمای سریع راه‌اندازی ربات

## مرحله 1: بررسی اولیه ✅

### 1.1 - بررسی PHP و افزونه‌ها
```bash
# از خط فرمان:
php -v
php -m | grep -E "pdo|curl|mbstring|json|openssl|zip"
```

یا از مرورگر:
```
http://localhost/test.php
```

### 1.2 - بررسی توکن ربات
```
http://localhost/test_bot.php
```

در این صفحه توکن ربات خود را وارد کنید و صحت آن را تأیید کنید.

---

## مرحله 2: نصب سیستم 🔧

### 2.1 - ایجاد پایگاه داده MySQL

1. وارد phpMyAdmin یا cPanel شوید
2. یک دیتابیس جدید ایجاد کنید (مثلاً: `telegram_bot`)
3. یک کاربر برای دیتابیس ایجاد کنید
4. دسترسی‌های FULL را به کاربر بدهید

### 2.2 - اجرای نصب‌کننده

```
http://localhost/install/
```

مراحل نصب:
1. **گام 1**: بررسی سیستم - تأیید PHP extensions
2. **گام 2**: تنظیم دیتابیس - اطلاعات MySQL
3. **گام 3**: ایجاد ادمین - نام کاربری و رمز عبور
4. **گام 4**: تنظیم ربات - توکن و URL سایت
5. **گام 5**: تکمیل - webhook خودکار تنظیم می‌شود

---

## مرحله 3: تنظیم Webhook 🔗

### 3.1 - Webhook خودکار (در نصب‌کننده)
Webhook به طور خودکار در مرحله 4 نصب تنظیم می‌شود.

### 3.2 - تنظیم دستی (اختیاری)
اگر نیاز به تنظیم مجدد دارید:

```
http://localhost/public/set_webhook.php
```

یا از پنل ادمین:
```
پنل مدیریت → وضعیت سیستم → تنظیم مجدد Webhook
```

### 3.3 - بررسی Webhook
```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/getWebhookInfo
```

---

## مرحله 4: تنظیمات ربات ⚙️

### 4.1 - ورود به پنل مدیریت
```
http://localhost/admin/
```

**نام کاربری و رمز عبور**: همان که در مرحله نصب ساختید

### 4.2 - افزودن کانال

1. برو به: **کانال‌ها** → **افزودن کانال جدید**
2. اطلاعات را وارد کن:
   - **Chat ID**: شناسه عددی کانال (مثلاً: `-1001234567890`)
   - **Username**: نام کاربری کانال (مثلاً: `testchannel`)
   - **فعال**: تیک بزن
   - **قالب اختصاصی**: خالی بگذار (از قالب پیش‌فرض استفاده می‌کند)

**💡 چگونه Chat ID کانال را پیدا کنیم؟**

روش 1: با @userinfobot
```
1. @userinfobot را به کانال اضافه کن
2. یک پیام forward کن از کانال به ربات
3. Chat ID را کپی کن
```

روش 2: از webhook
```
1. یک پست در کانال منتشر کن
2. در لاگ‌ها Chat ID را پیدا کن
```

### 4.3 - افزودن تگ‌ها و ID

1. برو به: **تگ‌ها و ID**
2. برای هر تگ:
   - نوع: `tag`
   - مقدار: `#برنامه_نویسی`
   - ترتیب: `0` (برای اولین تگ)
   - فعال: ✓

3. برای هر ID:
   - نوع: `id`
   - مقدار: `@channel1`
   - ترتیب: `0`
   - فعال: ✓

### 4.4 - تنظیم قالب پیام

1. برو به: **قالب پیام**
2. قالب دلخواه خود را وارد کن:

```
🔖 تگ‌ها: {tags}
🆔 کانال‌ها: {ids}

📅 تاریخ: {date}
🔗 لینک: {permalink}
```

**متغیرهای موجود:**
- `{tags}` - همه تگ‌های فعال
- `{ids}` - همه ID های فعال
- `{channel_username}` - نام کاربری کانال
- `{channel_id}` - شناسه عددی کانال
- `{message_id}` - شماره پیام
- `{date}` - تاریخ و ساعت
- `{post_type}` - نوع پست (text, photo, ...)
- `{permalink}` - لینک مستقیم به پست
- `{custom1}`, `{custom2}`, `{custom3}` - متغیرهای سفارشی

---

## مرحله 5: افزودن ربات به کانال 🤖

### 5.1 - افزودن ربات
1. وارد کانال خود شو
2. تنظیمات کانال → Administrators → Add Administrator
3. ربات خود را جستجو کن و اضافه کن

### 5.2 - تنظیم دسترسی‌ها
حداقل دسترسی‌های مورد نیاز:
- ✅ **Post Messages** - برای ارسال کامنت
- ✅ **Edit Messages** (اختیاری)
- ✅ **Delete Messages** (اختیاری)

---

## مرحله 6: تست ربات 🧪

### 6.1 - تست داخلی
```
http://localhost/test_webhook.php
```

این یک update جعلی به webhook می‌فرستد و پاسخ را نشان می‌دهد.

### 6.2 - تست واقعی
1. یک پست در کانال منتشر کن
2. ربات باید زیر پست کامنت بگذارد
3. اگر کامنت نگذاشت، لاگ‌ها را بررسی کن:
   ```
   پنل مدیریت → وضعیت سیستم
   ```

---

## عیب‌یابی 🔍

### مشکل: ربات کامنت نمی‌گذارد

**چک لیست:**
1. ✅ ربات در کانال Admin است؟
2. ✅ کانال در پنل اضافه شده؟
3. ✅ کانال فعال است؟
4. ✅ تگ‌ها/ID ها فعال هستند؟
5. ✅ قالب پیام تنظیم شده؟
6. ✅ Global Toggle فعال است؟
7. ✅ Webhook درست تنظیم شده؟

**بررسی لاگ‌ها:**
```
/logs/app-YYYY-MM-DD.log
```

یا از پنل:
```
پنل مدیریت → وضعیت سیستم → مشاهده لاگ‌ها
```

### مشکل: 401 Unauthorized

**علت**: Secret Token نامعتبر است

**راه حل:**
```
پنل مدیریت → وضعیت سیستم → تنظیم مجدد Webhook
```

### مشکل: 403 Forbidden

**علت**: ربات در کانال Admin نیست یا دسترسی ندارد

**راه حل:**
1. ربات را به کانال اضافه کنید
2. به عنوان Admin تنظیم کنید
3. دسترسی "Post Messages" را بدهید

### مشکل: 404 Not Found

**علت**: Webhook URL اشتباه است

**راه حل:**
```
1. بررسی کنید که BASE_URL در config/.env.php درست است
2. Webhook را مجدد تنظیم کنید
```

---

## پیکربندی پیشرفته 🔧

### تنظیم Parse Mode

در **تنظیمات**:
- **MarkdownV2**: برای استایل‌دهی با Markdown (پیشنهادی)
- **HTML**: برای استایل‌دهی با HTML

### محدودیت نرخ (Rate Limiting)

ربات به طور خودکار:
- از ارسال مجدد به یک پست جلوگیری می‌کند
- در صورت خطای 429 (Too Many Requests) retry می‌کند
- حداکثر 3 بار تلاش مجدد

### قالب اختصاصی برای هر کانال

می‌توانید برای هر کانال یک قالب جداگانه تنظیم کنید:
```
پنل مدیریت → کانال‌ها → ویرایش کانال → قالب اختصاصی
```

---

## Backup و بروزرسانی 💾

### Backup دستی
```
پنل مدیریت → مدیریت آپدیت → دانلود بکاپ
```

### بروزرسانی خودکار
```
پنل مدیریت → مدیریت آپدیت → بررسی آپدیت → نصب
```

بکاپ خودکار قبل از هر آپدیت ایجاد می‌شود.

---

## Production Checklist ✅

قبل از استفاده در محیط واقعی:

- [ ] توکن ربات در `.env.php` ایمن نگهداری شود
- [ ] پوشه `/install` را حذف یا محافظت کنید
- [ ] پوشه `/logs` را writable کنید (755)
- [ ] فایل `config/.env.php` را 600 کنید
- [ ] HTTPS را فعال کنید
- [ ] Backup منظم تنظیم کنید
- [ ] لاگ‌ها را به طور منظم بررسی کنید

---

## لینک‌های مفید 🔗

- 📖 [مستندات API تلگرام](https://core.telegram.org/bots/api)
- 🤖 [BotFather](https://t.me/BotFather)
- 📊 [صفحه تست](http://localhost/test.php)
- 🔄 [تست Webhook](http://localhost/test_webhook.php)
- 💻 [پنل مدیریت](http://localhost/admin/)
- 🔍 [صفحه Debug](http://localhost/admin/debug.php)

---

## پشتیبانی 💬

برای سوالات و مشکلات:
- 🐛 [GitHub Issues](https://github.com/hazhan4268/CHANNELTAG/issues)
- 📧 ایمیل پشتیبانی
- 💬 گروه تلگرام

---

**نسخه**: 2.1.0  
**آخرین بروزرسانی**: 2025-11-02